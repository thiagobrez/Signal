{
  "name": "signal",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && ./scripts/sync-version.sh",
    "tag": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  }
}
