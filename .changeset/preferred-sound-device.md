---
"signal": minor
---

Choose which output device plays each of Signal's sound cues (on open, on completion, on all done) in Preferences, so the auto-open chime can go to the built-in speakers even while headphones are the system default
