---
"signal": patch
---

Wrap long tasks onto up to three lines instead of scrolling them sideways, so the text stays aligned with the other rows. Past three lines the row scrolls its own text, and the arrow keys walk those lines before moving to the next task
