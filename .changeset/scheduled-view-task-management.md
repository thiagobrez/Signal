---
"signal": minor
---

Manage tasks straight from the Scheduled view: add, edit and remove them on today and on any day ahead, with the same rows, keyboard and delete button as the main Signal panel. Typing a keyword like "every monday" on a day schedules it there, so the frequency popover is gone; days already past stay read-only history
